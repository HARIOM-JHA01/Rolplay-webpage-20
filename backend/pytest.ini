[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure-logic tests, no DB, no network
    integration: hits a real test MongoDB (see docker-compose.test.yml), external HTTP mocked
    e2e: hits a fully built+running container over HTTP; requires Docker
addopts = -m "not e2e"
